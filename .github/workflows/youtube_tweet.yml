name: YouTube AI Tweet Generator

on:
  schedule:
    # Run at 6:00 AM JST daily (21:00 UTC)
    - cron: '0 21 * * *'
  workflow_dispatch:

jobs:
  youtube-ai-tweet:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Generate and Post YouTube AI Tweet
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
          TINYURL_API_KEY: ${{ secrets.TINYURL_API_KEY }}
          TWITTER_CONSUMER_KEY: ${{ secrets.TWITTER_CONSUMER_KEY }}
          TWITTER_CONSUMER_SECRET: ${{ secrets.TWITTER_CONSUMER_SECRET }}
          TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
          TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
        run: python youtube_tweet_generator.py

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: youtube-ai-tweet-logs-${{ github.run_number }}
          path: |
            *.log
          retention-days: 7

      - name: Notify on failure
        if: failure()
        run: echo "YouTube AI tweet generation failed. Check the logs for details."